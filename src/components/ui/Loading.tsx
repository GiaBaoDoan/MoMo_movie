const Loading = () => {
  return (
    <div className="flex justify-center items-center h-[100vh]">
      <span className="loading bg-pinkTheme loading-dots loading-lg"></span>
    </div>
  );
};

export default Loading;
